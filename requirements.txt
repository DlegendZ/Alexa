# core
langgraph>=1.0
ollama>=0.6
httpx>=0.28
python-dotenv>=1.0
chromadb>=1.0

# the airlock's summariser (deepseek, Anthropic-compatible endpoint)
anthropic>=0.60

# the airlock's web pipeline
ddgs>=9.0
trafilatura>=2.0

# dev
pytest>=8.0
